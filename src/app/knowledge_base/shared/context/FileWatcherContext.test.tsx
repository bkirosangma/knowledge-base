import { describe } from "vitest";

// Tests are rewritten in Task 10 (body-swap landed in Task 9; event-driven test
// suite arrives next). Keeping this file as a stub so typecheck passes.
describe.skip("FileWatcherContext (rewritten in Task 10)", () => {});
