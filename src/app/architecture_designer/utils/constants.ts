/** Gap between layers (minimum distance) */
export const LAYER_GAP = 10;

/** Gap between nodes (minimum distance) */
export const NODE_GAP = 8;

/** Padding inside a layer around its contained nodes */
export const LAYER_PADDING = 25;

/** Extra vertical offset for the layer title bar */
export const LAYER_TITLE_OFFSET = 20;

/** Default width for a newly created element */
export const DEFAULT_NODE_WIDTH = 210;

/** Default dimensions for a newly created layer */
export const DEFAULT_LAYER_WIDTH = 400;
export const DEFAULT_LAYER_HEIGHT = 200;
